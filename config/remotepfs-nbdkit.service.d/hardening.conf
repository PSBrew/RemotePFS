[Service]
# nbdkit child process needs /run/remotepfs writable and, via RuntimeDirectory,
# the dir is owned by User=/Group= (remotepfs-nbd:remotepfs), mode 0700.
# Socket mode enforced by systemd UMask=0077 (service-user-only).
CapabilityBoundingSet=
AmbientCapabilities=
